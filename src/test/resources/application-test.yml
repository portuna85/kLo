spring:
  datasource:
    url: jdbc:h2:mem:kraft_lotto_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

kraft:
  lotto:
    scheduler:
      enabled: false
    api:
      backfill-delay-ms: 0
  collect:
    auto:
      enabled: false
  skip:
    required-config-validator: true
  api:
    client: mock
    url: http://localhost
    connect-timeout-ms: 2000
    read-timeout-ms: 3000
    max-retries: 2
    retry-backoff-ms: 50
    fallback-to-mock-on-failure: false
    mock-latest-round: 1200
  admin:
    api-token: test-admin-token
    token-header: X-Kraft-Admin-Token
  recommend:
    max-attempts: 1000
    rate-limit:
      max-requests: 30
      window-seconds: 60
      recommend:
        max-requests: 30
        window-seconds: 60
      collect:
        max-requests: 30
        window-seconds: 60

logging:
  level:
    org.hibernate.orm.deprecation: ERROR
    org.flywaydb.core.internal.database.base.Database: ERROR
    org.flywaydb.core.internal.sqlscript.DefaultSqlScriptExecutor: ERROR
    org.springframework.boot.security.autoconfigure.UserDetailsServiceAutoConfiguration: ERROR
    org.mariadb.jdbc.message.server.ErrorPacket: ERROR
    org.hibernate.orm.jdbc.error: ERROR
